# セキュリティとプライバシー方針

## Secrets管理

AWSアクセスキー、AWSシークレットキー、モデルARNなどの環境固有情報は、ソースコードやREADMEに直接記載しません。ローカル実行では以下のどちらかで設定します。

- `.streamlit/secrets.toml`
- 環境変数

`.streamlit/secrets.toml`、`.env`、DB、動画、画像は`.gitignore`でGit管理対象から除外しています。

## 個人情報・動画データ

剣道の試合動画や画像には、顔、学校名、道場名、試合会場、対戦相手などの個人情報・機微情報が含まれる可能性があります。提出・公開時は、以下を確認します。

- 個人が特定できる画像・動画を含めない
- 利用許諾のない試合動画を含めない
- SQLite DBに実データや個人情報が残っていない
- AWSアカウントIDやモデルARNの実値が含まれていない

## 確認コマンド例

```bash
rg -n "arn:aws|AWS_SECRET_ACCESS_KEY|AKIA|中央大学|氏名|住所|電話|メール"
```
